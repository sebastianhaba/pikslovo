<UserControl
    x:Class="Pikslovo.Controls.ApiKeyInputControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid>
    <PasswordBox x:Name="PasswordInput"
                 Height="48"
                 Padding="12,0,48,0"
                 PasswordRevealMode="Hidden"
                 PasswordChanged="PasswordInput_PasswordChanged"
                 KeyDown="PasswordInput_KeyDown" />
    <Button Style="{StaticResource HeaderBackButton}"
            Click="RevealButton_Click"
            HorizontalAlignment="Right"
            VerticalAlignment="Center"
            Width="44"
            Height="44"
            Padding="0"
            ToolTipService.ToolTip="Pokaż lub ukryj klucz API">
      <Grid Width="20" Height="20">
        <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE890;" FontSize="18" />
        <Border x:Name="HideSlash"
                Width="22"
                Height="2"
                Background="{ThemeResource AppTextBrush}"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Visibility="Collapsed"
                RenderTransformOrigin="0.5,0.5">
          <Border.RenderTransform>
            <RotateTransform Angle="-45" />
          </Border.RenderTransform>
        </Border>
      </Grid>
    </Button>
  </Grid>
</UserControl>
